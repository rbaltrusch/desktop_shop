[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
	unreliable: marks tests as unreliable (deselect with '-m "not unreliable"')
	generate: marks database generation tests (deslect with '-m "not generate"')
testpaths =
	unit
